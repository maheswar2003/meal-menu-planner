# Environment variables for Meal Menu Planner Backend

# MongoDB Connection URI
# Example: mongodb+srv://<username>:<password>@yourcluster.mongodb.net/yourDatabaseName?retryWrites=true&w=majority
MONGODB_URI=

# Port for the backend server (defaults to 5000 if not set)
PORT=5000

# Node environment (development, production)
# Set to 'production' when deploying
NODE_ENV=development