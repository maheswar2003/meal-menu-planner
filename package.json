{
  "name": "meal-menu-planner",
  "version": "1.0.0",
  "description": "A full-stack web application for managing and displaying weekly meal menus.",
  "author": "maheswar2003 <your-email@example.com>",
  "license": "MIT",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && cd client && npm install && npm run build",
    "heroku-postbuild": "npm install && cd client && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
